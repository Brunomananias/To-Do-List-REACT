import React from 'react'
import Styles from './Footer.module.css'

const Footer = () => {
    return (
        <footer className={Styles.footer}>
        <p>
          <span>React + TS To do 2024</span>
        </p>
      </footer>
    );
};

export default Footer;

